{
  "name": "coffee-saving-point-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "concurrently -n server,client -c magenta,cyan \"cd server && npm start\" \"cd client && npm run preview\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
